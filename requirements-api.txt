# Slim requirements for the API server AND GitHub Actions pipeline.
# The heavy ML stack (torch, pymc, wandb) is excluded — too large for CI runners.
numpy>=1.26
pandas>=2.2
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.2
diskcache>=5.6
supabase>=2.0
